{% include "navbar.html" %}
   


    <div class="container">

        <div class="row">

            <div class="col-lg-12">
                <h1 class="page-header">CVG Blog
                    
                </h1>
                <ol class="breadcrumb">
                    <li><a href="index.html">Home</a>
                    </li>
                    <li class="active">Blog</li>
                </ol>
            </div>

        </div>

        <div class="row">

            <div class="col-lg-8">
				{% for post in posts %}
                <h1><a href="/blog/blogpost/{{post.slug}}">{{post.title}}</a>
                </h1>
                <p class="lead">by <a href="#">{{post.author}}</a>
                </p>
                <hr>
                <p><i class="fa fa-clock-o"></i> Posted on {{post.created_on}}</p>
                <hr>
                <a href="/blog/blogpost/{{post.slug}}">
                    <img src="http://placehold.it/900x300" class="img-responsive">
                </a>
                <hr>
                <p>{{post.slug}}This is a very basic starter template for a blog homepage. It makes use of Font Awesome icons that are built into the 'Modern Business' template, and it makes use of the Bootstrap 3 pager at the bottom of the page.</p>
                <a class="btn btn-primary"  href="/blog/blogpost/{{post.slug}}">Read More <i class="fa fa-angle-right"></i></a>

                <hr>
				{% endfor %}
                
                <ul class="pager">
                 {% if posts.has_next %} 
                    <li class="previous"><a href="?page={{ posts.next_page_number }}">&larr; Older</a>
                    </li>
                    {% endif %}
                    
                  {% if posts.has_previous %} 
                    <li class="next"><a href="?page={{ posts.previous_page_number }}">Newer &rarr;</a>
                    </li>
                  {% endif %}   
                </ul>

            </div>

            <div class="col-lg-4">
                <div class="well">
                    <h4>Blog Search</h4>
                    <div class="input-group">
                    <form action="/blog/search/" method=GET>
                    {% csrf_token %}
                        <input type="text" class="form-control" name="searchbox">
                        
                        <span class="input-group-btn">
                            <button class="btn btn-default" type="submit"><i class="fa fa-search"></i>
                            </button>
                        </span>
                        </form>
                    </div>
                    <!-- /input-group -->
                </div>
                <!-- /well -->
                
            </div>
        </div>

    </div>
    <!-- /.container -->

  {% include "copyrightcontainer.html" %}

   
</body>

</html>
